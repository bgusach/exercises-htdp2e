#lang htdp/isl

; ### Functions

; [List-of Number] [Number Number -> Boolean] [List-of Number]
(define (sort-n lon comparator)
  lon
  )


; [List-of String] [String String -> Boolean] -> [List-of String]
(define (sort-s los comparator)
  los
  )


; [X] [List-of X] [X X -> Boolean] -> [List-of X]

; If the previous signature gets instantiated with X=IR, we get:
; [List-of IR] [IR IR -> Boolean] -> [List-of IR]

